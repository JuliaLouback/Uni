@model Uni.Models.Cliente_Venda

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Cliente_Venda</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Cliente_Cpf" class="control-label"></label>
                <select asp-for="Cliente_Cpf" class ="form-control" asp-items="ViewBag.Cliente_Cpf"></select>
            </div>
            <div class="form-group">
                <label asp-for="Venda_Id_venda" class="control-label"></label>
                <select asp-for="Venda_Id_venda" class ="form-control" asp-items="ViewBag.Venda_Id_venda"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
