@model Uni.Models.Telefone

@{
    ViewData["Title"] = "Detalhes";
}

<h1 class="text-primary mt-2 mb-2">Detalhes</h1>

<div>
    <h4>Telefone</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Telefones)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Telefones)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-success" asp-action="Index">Voltar</a>
    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model.Id_telefone">Editar</a>

</div>
